{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (Simulator)",
            "type": "playdate",
            "request": "launch",
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}
